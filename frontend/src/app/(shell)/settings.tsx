import { PlaceholderScreen } from "@/src/ui/PlaceholderScreen";

export default function SettingsScreen() {
  return (
    <PlaceholderScreen
      testID="screen-settings"
      title="Settings"
      subtitle="Session environment, config batch selection and app preferences."
      icon="cog-outline"
    />
  );
}
