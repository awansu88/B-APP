import { PlaceholderScreen } from "@/src/ui/PlaceholderScreen";

export default function ExportScreen() {
  return (
    <PlaceholderScreen
      testID="screen-export"
      title="Export"
      subtitle="Export raw rounds, shoes and predictions on-device (JSON / CSV)."
      icon="export-variant"
    />
  );
}
