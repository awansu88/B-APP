import { PlaceholderScreen } from "@/src/ui/PlaceholderScreen";

export default function ActiveShoeScreen() {
  return (
    <PlaceholderScreen
      testID="screen-active-shoe"
      title="Active Shoe"
      subtitle="Live round entry (P / T / B) and roadmaps will live here."
      icon="cards-playing-outline"
    />
  );
}
