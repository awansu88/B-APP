import { PlaceholderScreen } from "@/src/ui/PlaceholderScreen";

export default function StatisticsScreen() {
  return (
    <PlaceholderScreen
      testID="screen-statistics"
      title="Statistics"
      subtitle="Distribution, streaks and recommendation performance."
      icon="chart-bar"
    />
  );
}
